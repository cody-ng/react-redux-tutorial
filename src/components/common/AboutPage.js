import React from "react";

const AboutPage = () => (
  <div>
    <h2>About</h2>
    <p>
      This app is created from the React Redux tutorial. I then added the
      redux-thunk library. After that, I want to use new hooks to share data.
    </p>
  </div>
);

export default AboutPage;
